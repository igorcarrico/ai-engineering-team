# ---- Web framework ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
sse-starlette>=2.1
python-multipart>=0.0.9

# ---- Settings & validation ----
pydantic>=2.9
pydantic-settings>=2.4
python-dotenv>=1.0

# ---- Persistence ----
SQLAlchemy>=2.0
aiosqlite>=0.20
greenlet>=3.0
# Postgres (used in docker-compose). Safe to keep installed locally.
asyncpg>=0.29

# ---- Orchestration / LLM ----
langgraph>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-openai>=1.0,<2.0
langchain-anthropic>=1.0,<2.0

# ---- Utilities ----
tenacity>=9.0
httpx>=0.27
structlog>=24.1
markdown>=3.7
pymdown-extensions>=10.7
